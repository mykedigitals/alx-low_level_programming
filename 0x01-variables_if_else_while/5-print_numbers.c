#include <stdio.h>

/**
 * main- declaring var,then printing digits
 * Return: 0
 */

int main(void)
{
	int numb;
		for (numb = 0; numb <= 9; numb++)
			{ printf("%d", numb);
				}

		printf("\n");
		return (0);

}
